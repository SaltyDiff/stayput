# Golden vectors

These files freeze T1 canonical bytes and `record_digest` values.

Do not regenerate them to “fix” a digest change. A changed golden means
canonicalization semantics changed and needs an explicit version decision.
