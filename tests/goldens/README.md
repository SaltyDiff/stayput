# Golden vectors

These files freeze T1 canonical bytes / `record_digest` values and T4
instruction-byte digests.

Do not regenerate them to “fix” a digest change. A changed golden means
canonicalization or instruction-digest semantics changed and needs an
explicit version decision.
